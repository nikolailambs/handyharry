<div class="panel-group" id="accordion">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h4 class="panel-title">
            <a data-toggle="collapse" href="#facets-1">
              <ul>
                <li>
                  <i class="fa fa-search" aria-hidden="true"></i>
<!--                   https://bootsnipp.com/snippets/featured/circle-progress-bar-->
                  <div class="progress-value">  <img class="avatar-large" src="https://kitt.lewagon.com/placeholder/users/gabriel-dehan"></div><!-- MUST BE STATUS LATER!! -->
                  <i class="fa fa-bell-o" aria-hidden="true"></i>
                  <div class="notification-icon">
                    <span class="glyphicon glyphicon-envelope"></span>
                    <span class="message-badge">19</span> <!-- MUST BE MESSAGES COUNNT LATER!! -->
                  </div>
                    <% if policy(Project).create? %>
                      <%= link_to '', new_project_path, class: "fa fa-cart-plus" %> <!-- to be added: aria-hidden="true" -->
                    <% end %>
                </li>
              </ul>
            </a>
          </h4>
        </div>
        <div id="facets-1" class="panel-collapse collapse in">
          <ul class="list-group">
            <div class="list-group-item checkbox checkbox-circle">
              <input type="text" name="search" id="searchbox" placeholder="Enter a search" />
            </div>
          </ul>
        </div>
      </div>

      <% @projects.each do |project| %>
      <div  class="myProgress" data-project-completion="<%= project.completion_percentage %>">
          <span class="progress-left">
          <span class="progress-bar"></span>
          </span>
          <span class="progress-right">
          <span class="progress-bar"></span>
          </span>

      </div>
      <div class="panel panel-default">
        <div class="panel-heading">
          <h4 class="panel-title">
            <a data-toggle="collapse" href="#facets-2"><span class="glyphicon glyphicon-tag"></span>
              <ul>
                <li>
                <img class="avatar-large" src="https://kitt.lewagon.com/placeholder/users/gabriel-dehan">
                <%= project.address %>
                <%= project.status %>

                <%= link_to "", project_path(project), class: "tag-link" %>
                </li>
              </ul>
            </a>
          </h4>
        </div>
        <div id="facets-2" class="panel-collapse collapse in">
          <ul class="list-group">
            <div class="list-group-item">
              <i class="fa fa-phone" aria-hidden="true"></i>
              <span class="glyphicon glyphicon-envelope"></span>
              <i class="fa fa-map-marker" aria-hidden="true"></i>
            </div>
          </ul>
        </div>
        <% if policy(Task).create? %>
          <div class="list-group-item checkbox checkbox-circle">
            <input type="checkbox" name="created" id="created2" />
            <label for="created2">
              <%= render 'task/index', project: @project %>
            <span class="badge">1</span></label>
          </div>
          <div class="list-group-item checkbox checkbox-circle">
            <input type="checkbox" name="created" id="created2" />
            <label for="created2">
              <%= render 'tasks/task_field', project: @project %>
            <span class="badge">1</span></label>
          </div>
        <% end %>
      <% end %>
      </div>



<%=  content_for :after_js do %>

<script>
var bar = $(".myProgress");

if (bar) {
function setProgress(){
  var status = bar

  if (status = 100)
    bar.className = "progress green";
  else if (status = 75)
    bar.className = "progress blue fifty";
  else if (status = 50)
    bar.className = "progress yellow";
  else if (status < 50)
    bar.className = "progress pink";
}

}

</script>

<% end %>




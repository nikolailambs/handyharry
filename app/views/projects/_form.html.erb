<%= simple_form_for @project do |f| %>
<!-- INPUT NICOLAI FOR PROJECT -->

  <%= f.input 'title' %>
  <%= f.input 'description' %>
  <%= f.input 'address' %>
  <%= f.input 'deadline' %>

  <div class="assign-to-handy-field">
    <h4>Assign the project to an handyman!</h4>
    <%= f.association :handy, :include_blank => false, collection: User.where(handy: true) %>
  </div>
  <%= f.button :submit %>
<% end %>
